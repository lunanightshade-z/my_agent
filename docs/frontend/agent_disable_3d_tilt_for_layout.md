## 背景
Agent 页面中间主面板应用了 3D 旋转（`rotateX/rotateY` + `perspective`），在视觉投影下会让输入框看起来“离底部有空隙”，并导致开发者工具测量到的 `main` 边界与内部子元素位置不直观。

## 处理
为保证布局稳定性与“输入框贴底”的一致体验，禁用中间主面板（`<motion.main>`）的 3D 旋转，仅保留其它装饰（背景光晕、阴影、毛玻璃）。

## 影响
- 中间主面板不再随鼠标产生 3D 倾斜
- 输入框贴底的视觉效果与 DOM 测量更一致

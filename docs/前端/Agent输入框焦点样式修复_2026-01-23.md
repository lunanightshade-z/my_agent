## 问题定位
- 黑色矩形来自全局焦点样式：`frontend/src/index.css` 中 `textarea:focus-visible` 使用 `outline: 2px solid currentColor`
- `currentColor` 在 Agent 输入框为深色文字色，导致黑色描边

## 修复方式
- `frontend/src/pages/Agent.jsx`：对该 textarea 覆盖 `focus-visible`，改为浅色发光描边

## 验证与部署
- `npm run build`（frontend）
- `docker compose up -d --build`

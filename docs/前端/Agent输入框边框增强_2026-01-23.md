## 变更概述
- 强化 Agent 页面输入框外层线条与光晕，提升可见度
- Chat 页面输入框边缘发光改为常显，弱化仅聚焦时的依赖

## 具体调整
- `frontend/src/pages/Agent.jsx`：为输入区域增加双层描边与柔和光晕
- `frontend/src/components/chat/InputContainer/InputContainer.module.css`：默认边框与阴影更明显

## 验证与部署
- `npm run build`（frontend）
- `docker compose up -d --build`

# 前端架构新手入门指南

> 本文档帮助刚接触项目的开发者快速理解前端架构、页面布局和样式修改方法。

---

## 📚 目录

1. [项目概述](#项目概述)
2. [前端架构概览](#前端架构概览)
3. [目录结构说明](#目录结构说明)
4. [两个页面的布局](#两个页面的布局)
5. [样式系统详解](#样式系统详解)
6. [如何修改样式](#如何修改样式)
7. [常见问题](#常见问题)

---

## 📖 项目概述

本项目是一个 LLM Agent 应用，主要包含两个聊天页面：

- **Chat 页面** (`/chat`) - 赛博朋克金色风格，深色背景
- **Agent 页面** (`/agent`) - 空气感淡色风格，浅色背景

两个页面功能相似（都是聊天对话），但**样式风格完全不同**，并且**样式系统完全隔离**，互不干扰。

---

## 🏗️ 前端架构概览

### 核心架构特点

```
┌─────────────────────────────────────────┐
│          App.jsx (路由入口)              │
│  ┌─────────────┐      ┌──────────────┐ │
│  │  Chat 页面  │      │ Agent 页面   │ │
│  │  (AppLayout)│      │  (独立布局)  │ │
│  └─────────────┘      └──────────────┘ │
│         │                    │          │
│    ┌────▼────┐         ┌─────▼─────┐   │
│    │Chat主题 │         │Agent主题  │   │
│    │(金色)   │         │(淡色)     │   │
│    └─────────┘         └──────────┘   │
└─────────────────────────────────────────┘
```

### 架构设计原则

1. **主题驱动**: 所有样式通过主题配置管理，而非硬编码
2. **样式隔离**: Chat 和 Agent 使用完全独立的样式系统
3. **组件化**: 每个页面有独立的组件目录
4. **CSS Modules**: 使用 CSS Modules 实现样式作用域隔离

---

## 📁 目录结构说明

### 核心目录

```
frontend/src/
├── App.jsx                    # 应用入口，路由配置
├── pages/                     # 页面组件
│   ├── Chat.jsx              # Chat 页面（占位，实际在 AppLayout）
│   ├── Agent.jsx             # Agent 页面（完整实现）
│   └── Home.jsx               # 首页
│
├── components/                # 组件目录
│   ├── chat/                 # ✨ Chat 页面专用组件
│   │   ├── ChatBubble/       # 聊天气泡组件
│   │   ├── ChatArea/         # 聊天区域组件
│   │   ├── InputContainer/  # 输入框容器组件
│   │   └── index.js          # 组件导出
│   │
│   ├── agent/                # ✨ Agent 页面专用组件（未来扩展）
│   │   └── ...
│   │
│   ├── shared/               # 共享组件
│   │   └── ThemeProvider.jsx # 主题提供者（核心）
│   │
│   ├── layout/               # 布局组件
│   │   └── AppLayout.jsx     # Chat 页面的布局容器
│   │
│   └── ui/                   # UI 基础组件（主题无关）
│       ├── Button.jsx
│       ├── Input.jsx
│       └── ...
│
├── styles/                    # 样式系统
│   ├── themes/               # ✨ 主题系统（核心）
│   │   ├── base.theme.js     # 基础主题工厂
│   │   ├── chat.theme.js     # Chat 页面主题配置
│   │   ├── agent.theme.js    # Agent 页面主题配置
│   │   └── index.js          # 主题导出
│   │
│   ├── tokens.js             # 设计令牌（颜色、间距等）
│   ├── globals.js            # 全局样式工具
│   └── utils.js              # 工具函数（cn 等）
│
├── store/                     # Redux 状态管理
└── services/                   # API 服务
```

### 关键文件说明

| 文件/目录 | 作用 | 重要性 |
|----------|------|--------|
| `styles/themes/` | 主题配置系统 | ⭐⭐⭐ 核心 |
| `components/shared/ThemeProvider.jsx` | 主题注入组件 | ⭐⭐⭐ 核心 |
| `components/chat/` | Chat 页面组件 | ⭐⭐ 重要 |
| `components/layout/AppLayout.jsx` | Chat 页面布局 | ⭐⭐ 重要 |
| `pages/Agent.jsx` | Agent 页面 | ⭐⭐ 重要 |

---

## 🎨 两个页面的布局

### Chat 页面 (`/chat`)

#### 布局结构

```
┌─────────────────────────────────────────────────┐
│              AppLayout (布局容器)                  │
│  ┌──────────┐  ┌──────────────┐  ┌──────────┐  │
│  │          │  │              │  │          │  │
│  │ 左侧     │  │   中间        │  │  右侧    │  │
│  │ 会话历史 │  │   聊天区域    │  │  Artifact│  │
│  │          │  │              │  │          │  │
│  │          │  │  ┌────────┐ │  │          │  │
│  │          │  │  │输入框   │ │  │          │  │
│  │          │  │  └────────┘ │  │          │  │
│  └──────────┘  └──────────────┘  └──────────┘  │
└─────────────────────────────────────────────────┘
```

#### 组件层级

```
App.jsx
  └── AppLayout (应用 Chat 主题)
      ├── ChatHistory (左侧会话历史)
      ├── ChatArea (中间聊天区域)
      │   └── ChatBubble (消息气泡)
      └── InputContainer (输入框容器)
```

#### 样式特点

- **背景**: 深黑色 (`#0f0f0f`)
- **主色调**: 金色系 (`#d4af37`)
- **风格**: 赛博朋克、科技感
- **组件**: `components/chat/` 目录下的组件

### Agent 页面 (`/agent`)

#### 布局结构

```
┌─────────────────────────────────────────────────┐
│              Agent 页面 (独立布局)                 │
│  ┌──────────┐  ┌──────────────┐  ┌──────────┐  │
│  │          │  │              │  │          │  │
│  │ 左侧     │  │   中间        │  │  右侧    │  │
│  │ 时光胶囊 │  │   对话舞台    │  │ 灵感碎片  │  │
│  │ (历史)   │  │  (消息列表)   │  │ (上下文) │  │
│  │          │  │              │  │          │  │
│  │          │  │  ┌────────┐ │  │          │  │
│  │          │  │  │输入框   │ │  │          │  │
│  │          │  │  └────────┘ │  │          │  │
│  └──────────┘  └──────────────┘  └──────────┘  │
└─────────────────────────────────────────────────┘
```

#### 组件层级

```
App.jsx
  └── Agent (应用 Agent 主题)
      ├── 左侧历史记录 (内联实现)
      ├── 中间消息区域 (内联实现)
      └── 右侧上下文 (内联实现)
```

#### 样式特点

- **背景**: 浅米白色 (`#fdfcf8`)
- **主色调**: 青色系 (`#14b8a6`)
- **风格**: 空气感、清新
- **组件**: 直接在 `pages/Agent.jsx` 中实现

---

## 🎨 样式系统详解

### 主题系统架构

```
主题配置 (chat.theme.js / agent.theme.js)
    ↓
ThemeProvider (注入主题)
    ↓
CSS 变量 (--color-primary, --space-md 等)
    ↓
CSS Modules (组件样式文件)
    ↓
组件 (使用样式类名)
```

### 主题配置结构

每个主题包含以下配置：

```javascript
{
  colors: {
    primary: '#颜色值',        // 主色调
    secondary: '#颜色值',      // 次要色
    background: '#颜色值',     // 背景色
    text: {
      primary: '#颜色值',      // 主要文字色
      secondary: '#颜色值',    // 次要文字色
    },
    // ...
  },
  spacing: {
    xs: '4px',
    sm: '8px',
    md: '12px',
    // ...
  },
  typography: { /* 字体配置 */ },
  borderRadius: { /* 圆角配置 */ },
  // ...
}
```

### CSS Modules 工作原理

CSS Modules 会自动为类名添加唯一标识符，实现样式隔离：

```css
/* ChatBubble.module.css */
.bubble {
  background: var(--color-background);
}
```

编译后：

```css
.ChatBubble_bubble__abc123 {
  background: var(--color-background);
}
```

---

## ✏️ 如何修改样式

### 场景 1: 修改 Chat 页面的颜色

#### 步骤 1: 找到主题配置文件

```bash
frontend/src/styles/themes/chat.theme.js
```

#### 步骤 2: 修改颜色值

```javascript
// chat.theme.js
export const chatTheme = createTheme({
  colors: {
    primary: '#d4af37',        // ← 修改这里，比如改成 '#ff6b6b'
    secondary: '#e8d9c3',
    background: '#0f0f0f',
    // ...
  },
  // ...
});
```

#### 步骤 3: 保存并查看效果

修改后，所有使用 `var(--color-primary)` 的地方都会自动更新。

---

### 场景 2: 修改 Chat 页面的消息气泡样式

#### 步骤 1: 找到组件样式文件

```bash
frontend/src/components/chat/ChatBubble/ChatBubble.module.css
```

#### 步骤 2: 修改样式

```css
/* ChatBubble.module.css */
.bubble {
  /* 修改背景 */
  background: linear-gradient(135deg, rgba(212, 175, 55, 0.15) 0%, rgba(232, 217, 195, 0.05) 100%);
  
  /* 修改圆角 */
  border-radius: var(--radius-xl);  /* 或直接写: 24px */
  
  /* 修改内边距 */
  padding: var(--space-lg);  /* 或直接写: 16px */
  
  /* 添加阴影 */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}
```

#### 步骤 3: 保存并查看效果

---

### 场景 3: 修改 Agent 页面的颜色

#### 步骤 1: 找到主题配置文件

```bash
frontend/src/styles/themes/agent.theme.js
```

#### 步骤 2: 修改颜色值

```javascript
// agent.theme.js
export const agentTheme = createTheme({
  colors: {
    primary: '#14b8a6',        // ← 修改这里
    background: '#fdfcf8',     // ← 修改背景色
    // ...
  },
  // ...
});
```

---

### 场景 4: 修改 Agent 页面的消息气泡样式

#### 步骤 1: 找到 Agent 页面文件

```bash
frontend/src/pages/Agent.jsx
```

#### 步骤 2: 找到消息气泡的样式定义

在 `Agent.jsx` 中，消息气泡的样式是内联定义的：

```jsx
// Agent.jsx (约第 351-357 行)
<div className={`
  relative max-w-[80%] md:max-w-[70%] rounded-3xl backdrop-blur-md border shadow-sm
  ${msg.role === 'user' 
    ? 'bg-slate-800/5 border-slate-200/50 text-slate-700 rounded-br-none p-6' 
    : 'bg-white/70 border-white/60 text-slate-600 rounded-tl-none shadow-indigo-100/50 p-6'
  }
`}>
```

#### 步骤 3: 修改样式

可以直接修改 Tailwind 类名，或者提取为 CSS Modules：

**方式 A: 直接修改 Tailwind 类名**

```jsx
// 修改用户消息背景色
? 'bg-blue-100 border-blue-200/50 text-blue-700 rounded-br-none p-6'
```

**方式 B: 创建 CSS Modules（推荐）**

1. 创建 `frontend/src/components/agent/AgentBubble/AgentBubble.module.css`
2. 将样式迁移到 CSS Modules
3. 在组件中引用

---

### 场景 5: 修改全局样式（两个页面都生效）

#### 步骤 1: 找到全局样式文件

```bash
frontend/src/index.css
```

#### 步骤 2: 修改全局样式

```css
/* index.css */

/* 修改滚动条样式 */
::-webkit-scrollbar-thumb {
  background: rgba(0, 0, 0, 0.3);  /* ← 修改这里 */
}

/* 修改选择文本样式 */
::selection {
  background: rgba(0, 0, 0, 0.3);  /* ← 修改这里 */
}
```

---

## 📝 样式修改最佳实践

### ✅ 推荐做法

1. **使用主题变量**: 优先使用 `var(--color-primary)` 而非硬编码颜色
2. **CSS Modules**: 组件样式使用 CSS Modules，避免全局污染
3. **主题配置**: 颜色、间距等设计令牌在主题文件中统一管理
4. **保持隔离**: Chat 和 Agent 的样式互不干扰

### ❌ 避免做法

1. **硬编码颜色**: 不要在组件中直接写 `#d4af37`
2. **全局样式污染**: 不要使用全局类名影响其他组件
3. **直接修改 tokens.js**: 不要直接修改 `tokens.js`，应该在主题文件中覆盖

---

## 🔍 快速定位样式文件

### Chat 页面样式文件速查表

| 要修改的内容 | 文件路径 |
|------------|---------|
| 整体颜色主题 | `styles/themes/chat.theme.js` |
| 消息气泡样式 | `components/chat/ChatBubble/ChatBubble.module.css` |
| 聊天区域样式 | `components/chat/ChatArea/ChatArea.module.css` |
| 输入框样式 | `components/chat/InputContainer/InputContainer.module.css` |
| 布局样式 | `components/layout/AppLayout.jsx` |

### Agent 页面样式文件速查表

| 要修改的内容 | 文件路径 |
|------------|---------|
| 整体颜色主题 | `styles/themes/agent.theme.js` |
| 消息气泡样式 | `pages/Agent.jsx` (内联样式，约 351-357 行) |
| 布局样式 | `pages/Agent.jsx` (整个文件) |
| 背景动画 | `pages/Agent.jsx` (AmbientBackground 组件) |

---

## 🎯 实战示例

### 示例 1: 将 Chat 页面的主色调改为蓝色

**步骤 1**: 打开 `frontend/src/styles/themes/chat.theme.js`

**步骤 2**: 修改 `primary` 颜色

```javascript
export const chatTheme = createTheme({
  colors: {
    primary: '#3b82f6',  // 从 #d4af37 改为蓝色
    // ...
  },
});
```

**步骤 3**: 保存文件，刷新页面查看效果

---

### 示例 2: 调整 Chat 页面消息气泡的内边距

**步骤 1**: 打开 `frontend/src/components/chat/ChatBubble/ChatBubble.module.css`

**步骤 2**: 修改 `.bubble` 的 `padding`

```css
.bubble {
  padding: var(--space-xl);  /* 从 var(--space-lg) 改为 var(--space-xl) */
  /* 或者直接写: padding: 24px; */
}
```

**步骤 3**: 保存文件，刷新页面查看效果

---

### 示例 3: 修改 Agent 页面的背景色

**步骤 1**: 打开 `frontend/src/styles/themes/agent.theme.js`

**步骤 2**: 修改 `background` 颜色

```javascript
export const agentTheme = createTheme({
  colors: {
    background: '#ffffff',  // 从 #fdfcf8 改为纯白色
    // ...
  },
});
```

**步骤 3**: 保存文件，刷新页面查看效果

---

## ❓ 常见问题

### Q1: 修改了主题文件，但页面没有变化？

**A**: 检查以下几点：
1. 确保修改的是正确的主题文件（Chat 用 `chat.theme.js`，Agent 用 `agent.theme.js`）
2. 确保组件使用了 CSS 变量（`var(--color-primary)`）而非硬编码
3. 清除浏览器缓存并刷新页面
4. 检查浏览器控制台是否有错误

---

### Q2: 如何知道某个样式属于哪个页面？

**A**: 查看文件路径：
- `components/chat/` → Chat 页面
- `pages/Agent.jsx` → Agent 页面
- `styles/themes/chat.theme.js` → Chat 主题
- `styles/themes/agent.theme.js` → Agent 主题

---

### Q3: 可以同时修改两个页面的样式吗？

**A**: 可以，但需要分别修改：
- Chat 页面: 修改 `chat.theme.js` 和 `components/chat/` 下的组件
- Agent 页面: 修改 `agent.theme.js` 和 `pages/Agent.jsx`

如果要修改全局样式（如滚动条），修改 `index.css`。

---

### Q4: CSS Modules 的类名在哪里定义？

**A**: CSS Modules 的类名定义在组件的 `.module.css` 文件中：

```
组件文件: components/chat/ChatBubble/ChatBubble.jsx
样式文件: components/chat/ChatBubble/ChatBubble.module.css  ← 这里
```

---

### Q5: 如何添加新的主题变量？

**A**: 在主题配置文件中添加：

```javascript
// chat.theme.js
export const chatTheme = createTheme({
  colors: {
    // ... 现有颜色
    myNewColor: '#ff0000',  // ← 添加新颜色
  },
  custom: {
    // 自定义属性
    myCustomProp: 'value',
  },
});
```

然后在 `ThemeProvider.jsx` 中添加 CSS 变量映射（如果需要）。

---

### Q6: 为什么 Agent 页面没有独立的组件目录？

**A**: Agent 页面目前是在 `pages/Agent.jsx` 中直接实现的。未来可以重构为独立的组件目录结构，就像 Chat 页面一样。

---

## 📚 进一步学习

### 相关文档

- [重构方案文档](./style_refactor_plan.md) - 详细的架构设计
- [重构完成报告](./style_refactor_completed.md) - 重构实施细节

### 推荐阅读顺序

1. ✅ 本文档（新手入门指南）
2. 📖 [重构方案文档](./style_refactor_plan.md) - 了解设计思路
3. 📖 [重构完成报告](./style_refactor_completed.md) - 了解实施细节

---

## 🎓 总结

### 核心要点

1. **两个页面，两套主题**: Chat（金色）和 Agent（淡色）完全独立
2. **主题驱动**: 颜色、间距等通过主题文件配置
3. **CSS Modules**: 组件样式使用 CSS Modules 隔离
4. **修改位置**: 
   - Chat → `styles/themes/chat.theme.js` + `components/chat/`
   - Agent → `styles/themes/agent.theme.js` + `pages/Agent.jsx`

### 快速修改流程

```
要修改 Chat 页面样式？
  ├─ 改颜色？ → 修改 chat.theme.js
  ├─ 改组件样式？ → 修改 components/chat/xxx/xxx.module.css
  └─ 改布局？ → 修改 components/layout/AppLayout.jsx

要修改 Agent 页面样式？
  ├─ 改颜色？ → 修改 agent.theme.js
  └─ 改组件样式？ → 修改 pages/Agent.jsx
```

---

**祝 coding 愉快！** 🚀

如有问题，请查看相关文档或询问团队成员。

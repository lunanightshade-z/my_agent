# My Agent 前端空白页面调试说明

## 🔍 问题诊断

如果访问 `http://8.130.129.37:28888` 看到空白页面，请按以下步骤排查：

### 1. 检查浏览器控制台

**打开浏览器开发者工具** (F12)，查看：
- **Console标签**: 是否有JavaScript错误
- **Network标签**: 检查资源加载情况
  - index.html 是否返回 200
  - JS文件 (index-ClsNHCJV.js) 是否加载成功
  - CSS文件 (index-DJhgio2t.css) 是否加载成功
  - API请求是否成功

### 2. 常见问题

#### 问题1: CORS错误
**症状**: 控制台显示 `CORS policy` 错误

**解决**: CORS已配置，如果仍有问题，检查：
```bash
# 查看API日志
docker logs ai_agent_api | grep CORS
```

#### 问题2: JavaScript文件加载失败
**症状**: Network标签显示JS文件404或加载失败

**解决**: 
```bash
# 重新构建前端
cd /home/superdev/my_agent/frontend
npm run build

# 重启nginx
cd ../backend/docker
docker compose restart nginx
```

#### 问题3: API请求失败
**症状**: 控制台显示API请求错误

**解决**:
```bash
# 检查API服务
docker logs ai_agent_api

# 测试API
curl http://localhost:28888/api/conversations
```

### 3. 强制刷新浏览器

**清除缓存并强制刷新**:
- Windows/Linux: `Ctrl + Shift + R` 或 `Ctrl + F5`
- Mac: `Cmd + Shift + R`

### 4. 检查服务状态

```bash
cd /home/superdev/my_agent/backend/docker
docker compose ps

# 应该看到所有服务都是 Up 状态
```

### 5. 查看完整日志

```bash
# 查看nginx日志
docker logs ai_agent_nginx -f

# 查看API日志
docker logs ai_agent_api -f

# 在浏览器访问时，观察日志输出
```

### 6. 验证文件完整性

```bash
# 检查前端文件
docker exec ai_agent_nginx ls -la /usr/share/nginx/html/
docker exec ai_agent_nginx ls -la /usr/share/nginx/html/assets/

# 应该看到:
# - index.html
# - assets/index-ClsNHCJV.js
# - assets/index-DJhgio2t.css
```

### 7. 测试API连接

在浏览器控制台执行：
```javascript
fetch('/api/conversations')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error)
```

如果返回 `{"conversations":[]}` 说明API正常。

---

## 🛠️ 快速修复

如果以上都正常但页面仍空白，尝试：

```bash
# 1. 完全重启所有服务
cd /home/superdev/my_agent/backend/docker
docker compose down
docker compose up -d

# 2. 等待服务启动（约10秒）
sleep 10

# 3. 验证服务
docker compose ps

# 4. 清除浏览器缓存并重新访问
```

---

## 📞 获取帮助

如果问题仍未解决，请提供：
1. 浏览器控制台的完整错误信息
2. Network标签中失败的请求详情
3. 服务器日志: `docker logs ai_agent_nginx` 和 `docker logs ai_agent_api`
